apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "kbot-1.0.13.tgz.fullname" . }}-test-connection"
  labels:
    {{- include "kbot-1.0.13.tgz.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "kbot-1.0.13.tgz.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
